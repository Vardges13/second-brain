# Second Brain - Система управления знаниями

Современное веб-приложение для управления памятью, документами и задачами с темной темой и русским интерфейсом.

## 🚀 Возможности

- **🧠 Память** — Просмотр и поиск записей из памяти с поддержкой Markdown
- **📄 Документы** — Управление документами с полнотекстовым поиском  
- **✅ Задачи** — Канбан-доска для управления задачами
- **🔍 Поиск** — Универсальный поиск по всему контенту с подсветкой
- **📊 Дашборд** — Обзор активности и быстрые действия

## 🛠 Технологии

- **Next.js 14+** с App Router
- **TypeScript** для типобезопасности
- **Tailwind CSS** для стилизации
- **react-markdown** для рендеринга Markdown
- **Lucide React** для иконок
- Статический экспорт для GitHub Pages

## 📋 Установка и запуск

### Требования
- Node.js 18+ 
- npm или yarn

### Локальная разработка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd second-brain

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
# Создать production сборку
npm run build

# Локально протестировать статическую версию
npm start
```

### Деплой на GitHub Pages

1. **Настроить GitHub репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <your-github-repo>
   git push -u origin main
   ```

2. **Настроить GitHub Pages:**
   - Перейти в Settings → Pages
   - Source: GitHub Actions
   - Создать файл `.github/workflows/deploy.yml`:

   ```yaml
   name: Deploy to GitHub Pages

   on:
     push:
       branches: ["main"]
     workflow_dispatch:

   permissions:
     contents: read
     pages: write
     id-token: write

   concurrency:
     group: "pages"
     cancel-in-progress: false

   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - name: Checkout
           uses: actions/checkout@v4
         - name: Setup Node
           uses: actions/setup-node@v4
           with:
             node-version: "20"
             cache: npm
         - name: Setup Pages
           uses: actions/configure-pages@v4
           with:
             static_site_generator: next
         - name: Install dependencies
           run: npm ci
         - name: Build with Next.js
           run: npm run build
           env:
             GITHUB_PAGES: true
         - name: Upload artifact
           uses: actions/upload-pages-artifact@v3
           with:
             path: ./out

     deploy:
       environment:
         name: github-pages
         url: ${{ steps.deployment.outputs.page_url }}
       runs-on: ubuntu-latest
       needs: build
       steps:
         - name: Deploy to GitHub Pages
           id: deployment
           uses: actions/deploy-pages@v4
   ```

3. **Пуш изменений:**
   ```bash
   git add .
   git commit -m "Add GitHub Pages deployment"
   git push
   ```

GitHub Actions автоматически соберет и задеплоит приложение.

## 📁 Структура проекта

```
src/
├── app/                    # App Router страницы
│   ├── page.tsx           # Дашборд
│   ├── memory/            # Страница памяти
│   ├── documents/         # Страница документов  
│   ├── tasks/             # Страница задач
│   ├── search/            # Страница поиска
│   ├── layout.tsx         # Корневой layout
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   └── Sidebar.tsx        # Боковая навигация
└── lib/
    └── data.ts           # Типы и sample данные
```

## 🎨 Кастомизация

### Темная тема
Приложение использует темную тему по умолчанию. Цвета определены в `globals.css`:
- Фон: `bg-gray-950` 
- Карточки: `bg-gray-900`
- Акценты: синий, зеленый, желтый, фиолетовый

### Данные
В текущей версии используются sample данные из `src/lib/data.ts`. Для подключения реальных данных:

1. Замените функции в `data.ts` на API вызовы
2. Добавьте состояние загрузки
3. Реализуйте CRUD операции

### Компоненты
Все UI компоненты используют Tailwind CSS классы. Кастомные компоненты определены в `globals.css`.

## 🔧 Скрипты

- `npm run dev` — Запуск dev сервера
- `npm run build` — Production сборка  
- `npm run start` — Запуск production версии
- `npm run lint` — ESLint проверка

## 📱 Адаптивность

Приложение полностью адаптивное:
- **Mobile**: Складывающееся меню, вертикальная прокрутка
- **Tablet**: Адаптивная сетка колонок
- **Desktop**: Полная функциональность

## 🚩 Известные ограничения MVP

- Данные хранятся в памяти (не персистентные)
- Нет аутентификации
- Нет редактирования контента
- Поиск работает только по sample данным

## 🛣 Roadmap

- [ ] Подключение к реальным файлам markdown
- [ ] CRUD операции для всех типов контента  
- [ ] Система тегов и категорий
- [ ] Экспорт/импорт данных
- [ ] Темы и кастомизация UI
- [ ] Мобильное приложение

## 📄 Лицензия

MIT License

## 🤝 Contributing

1. Fork репозиторий
2. Создайте feature ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)  
5. Откройте Pull Request

---

Создано для эффективной организации знаний и повышения продуктивности ⚡