/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  basePath: '/second-brain',
  assetPrefix: '/second-brain',
};

export default nextConfig;